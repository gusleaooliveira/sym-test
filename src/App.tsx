import React from "react";

function App() {
  return (
    <button
      onClick={() => {
        alert("Exemplo inicializado com sucesso!");
      }}
    >
      Clique!
    </button>
  );
}

export default App;
